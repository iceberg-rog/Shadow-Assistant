# Copy to .env and set strong values:
#   cp .env.example .env

# Dashboard login password
DASH_ADMIN_PASS=change-me-to-a-strong-password

# Flask session secret (any long random string)
FLASK_SECRET=change-me-to-a-long-random-hex-string
