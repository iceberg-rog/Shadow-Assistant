# راهنمای استفاده — Fleet Panel

داشبوردی برای راه‌اندازیِ خودکارِ سرورهای پروکسی. یک سرور (IP + دسترسی + نقش) می‌دهی،
خودش SSH می‌زند، نصب می‌کند، تست می‌گیرد و آدرس پنل + یوزر/پسورد را تحویل می‌دهد.

---

## ۰) بالا آوردن داشبورد
- **ویندوز:** دابل‌کلیک روی `run.bat` (یا `powershell -ExecutionPolicy Bypass -File run.ps1`)
- **لینوکس:** `./run.sh`

داشبورد می‌آید روی **http://localhost:8088** و رمز ادمین را خودش می‌سازد (داخل `.env`).

## ۱) ورود
آدرس بالا را باز کن و با رمزی که لانچر نشان داد وارد شو.

## ۲) کلید عمومی (یک‌بار برای هر سرور)
از منوی بالا **🔑 کلید عمومی** را باز کن و کلید را کپی کن. دو راه برای دسترسی بدون پسورد:
- در پنل سرویس‌دهنده (مثلاً **Cloudzy → SSH Keys**) این کلید را اضافه کن و موقع ساخت VPS انتخابش کن، یا
- روی سرور: `echo "<کلید>" >> ~/.ssh/authorized_keys`

> یا اصلاً کاری نکن: موقع افزودن سرور، **پسورد روت** را یک‌بار بده — داشبورد کلیدش را نصب می‌کند و پسورد را دور می‌اندازد.

## ۳) افزودن «سرور خارج» (اول این)
**➕ سرور جدید** → نقش: **خارج — اکسیت + پنل** → نام دلخواه، IP، یوزر `root`، پورت `22`،
و در صورت نیاز پسورد روت → **اتصال و نصب**.

داشبورد لاگِ زنده نشان می‌دهد. وقتی وضعیت **ready** شد، در صفحه‌ی همان سرور:
- **آدرس پنل Marzban**، **یوزر** و **پسورد** را می‌بینی.

## ۴) افزودن «سرور ایران» (بعد از خارج)
**➕ سرور جدید** → نقش: **ایران — relay** → علاوه بر IP/دسترسی، **IP اکسیت خارج** را هم بده
(همان سروری که در مرحله‌ی ۳ نصب شد). relay نصب می‌شود و به اکسیت وصل می‌شود.

> ⚠️ ترتیب مهم است: **همیشه اول خارج، بعد ایران.**

## ۵) ساخت کانفیگ برای مشتری
وارد **پنل Marzban** (آدرسی که در مرحله‌ی ۳ گرفتی) شو و برای هر مشتری یک کاربر با
**حجم** و **تاریخ انقضا** بساز؛ لینک/ساب‌اسکریپشن را تحویلش بده.

---

## نکات امنیتی
- داشبورد **کلید دسترسی به کل سرورهایت** را دارد؛ پشت فایروال/TLS نگهش دار و به اینترنت باز نگذار.
- پوشه‌ی `data/` (کلید خصوصی + دیتابیس) را هرگز کامیت نکن (در `.gitignore` هست).

## محدودیت‌های فعلی
- **نقش ایران (تونل خودکار):** هنوز در حال تکمیل است.
- **جئوبلاک داکر:** داشبورد و نودهای ایران را روی ماشینِ خارج از ایران اجرا کن (IP ایران از Docker Hub بلاک است).
- پنل Marzban گواهی **self-signed** دارد؛ مرورگر اخطار می‌دهد (Proceed بزن) یا یک دامنه وصل کن.
